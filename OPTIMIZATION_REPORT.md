# Project Optimization Report: azvirt_dms

## Executive Summary
A deep scan of the `azvirt_dms` project directory was performed using checksum verification and modification history analysis. The scan identified significant redundancy caused by build artifacts (`dist/` folder) being tracked or present in the workspace, as well as opportunities for reorganizing core logic to improve maintainability and storage efficiency.

---

## 1. Duplicate File Analysis (Checksum Verified)

The following sets of files have identical MD5 checksums. Most duplicates are between the source `client/public/` directory and the `dist/public/` build directory.

| Checksum | File 1 (Source/Primary) | File 2 (Duplicate) | Recommendation |
| :--- | :--- | :--- | :--- |
| `0f6d8b6b...` | `./client/public/sw.js` | `./dist/public/sw.js` | **Purge**: `dist/` should be gitignored and excluded from scans. |
| `24451388...` | `./client/public/_redirects` | `./dist/public/_redirects` | **Purge**: Delete duplicate in `dist/`. |
| `53ae8aa4...` | `./client/public/azvirt-logo.png` | `./dist/public/azvirt-logo.png` | **Purge**: Delete duplicate in `dist/`. |
| `89dd47cd...` | `./client/public/azvirt-35years-bg.png` | `./dist/public/azvirt-35years-bg.png` | **Purge**: Delete duplicate in `dist/`. |
| `efde478d...` | `./client/public/azvirt-bg.png` | `./dist/public/azvirt-bg.png` | **Purge**: Delete duplicate in `dist/`. |
| `d56bcfd2...` | `./dist/public/assets/classDiagram...js` | `./dist/public/assets/classDiagram-v2...js` | **Redundant**: Multiple versions of mermaid diagrams in build. |

**Actionable Step**: Add `dist/` to `.gitignore` if not already present, and ensure it is cleared before new builds to prevent stale artifact accumulation.

---

## 2. Redundant & Obsolete Assets

Based on modification history (files not touched since Dec 2025), the following assets and components are candidates for archiving or review:

### Potential Stale Assets
- **Images**: `client/public/azvirt-35years-bg.png`, `azvirt-bg.png`, `azvirt-logo.png` (Last modified 2025-12-27).
- **Plan Files**: `AI_ASSISTANT_PLAN.md` (Last modified 2025-12-27). This file may be obsolete if the implementation has moved forward.

### Redundant Components
- There is a high density of UI components in `client/src/components/ui/`. While many are standard, some like `client/src/components/ui/button-group.tsx` and `client/src/components/ui/item.tsx` might be underutilized or replaceable by more standard primitive sets.

---

## 3. Proposed File Structure Reorganization

To maximize storage efficiency and improve project performance/navigation, the following reorganization is proposed:

### A. Modularize Server Logic
Currently, `server/` contains many test files mixed with logic, and `server/_core/` is quite flat.
- **Move**: `server/*.test.ts` to `server/__tests__/`.
- **Group**: Logic in `server/routers/` by domain (e.g., `procurement/`, `logistics/`, `analytics/`).

### B. Consolidate Shared Resources
- **Move**: `client/src/_core/` and `server/_core/` share similar naming but different purposes.
- **Create**: A top-level `packages/shared-core` or similar if the project moves to a monorepo, or better distinguish between `client/lib` and `server/lib`.

### C. Optimize Public Assets
- **Action**: Compress `client/public/*.png` files. The current background images are ~1.2MB - 1.4MB each. Using WebP or AVIF could reduce these by 70-80%.

---

## 4. Action Plan

1. [ ] **Cleanup Build Artifacts**: Run `rm -rf dist/` and ensure it is properly ignored.
2. [ ] **Archive Obsolete Docs**: Move `AI_ASSISTANT_PLAN.md` to an `archive/` folder.
3. [ ] **Asset Optimization**: Convert PNG backgrounds to WebP format.
4. [ ] **Test Reorganization**: Move all `server/*.test.ts` files to a dedicated test directory to reduce noise in the logic folders.
5. [ ] **Component Audit**: Use a tool like `knip` to find unused exports and components in `client/src/components/`.

---
*Report generated by Kilo Code - 2026-02-28*
